### This file was generated by Nexus Schema
### Do not make changes to this file directly


input AddUserInput {
  email: String!
  id: ID
  password: String!
}

type Alternative {
  id: ID!
  text: String!
  votation: Votation
  votationId: String!
  votes: [Vote]
}

input CreateMeetingInput {
  description: String
  startTime: DateTime!
  title: String!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

union GetUserResult = User | UserNotFoundError

enum MajorityType {
  QUALIFIED
  SIMPLE
}

type Meeting {
  description: String
  id: ID!
  owner: User!
  participants: [Participant]!
  startTime: DateTime!
  status: Status!
  title: String!
  votations: [Votation]
}

type Mutation {
  castVote(alternativeId: String!, votationId: String!): Vote

  """"""
  createMeeting(meeting: CreateMeetingInput!): Meeting
}

type Participant {
  isVotingEligible: Boolean!
  role: Role!
  user: User
}

type Query {
  alternativesByVotation(votationId: String!): [Alternative]

  """Find meetings you are participating in"""
  meetings: [Meeting]!

  """Find a meeting by id from YOUR meetings"""
  meetingsById(meetingId: String!): Meeting
  user(id: ID!): GetUserResult
  userByEmail(email: String!): GetUserResult
  votationsByMeeting(meetingId: String!): [Votation]
}

enum Role {
  ADMIN
  COUNTER
  PARTICIPANT
}

enum Status {
  ENDED
  ONGOING
  UPCOMING
}

type User {
  email: String!
  emailVerified: Boolean!
  id: ID!
}

type UserNotFoundError {
  message: String!
}

type Votation {
  alternatives: [Alternative]
  blankVotes: Boolean
  description: String!
  hasVoted: [User]
  id: ID!
  majorityThreshold: Int!
  majorityType: MajorityType!
  meeting: Meeting!
  meetingId: String!
  order: Int
  status: Status!
  title: String!
}

type Vote {
  alternative: Alternative
  alternativeId: String!
  id: ID!
  nextVote: Vote
  nextVoteId: String
  prevVote: Vote
}
